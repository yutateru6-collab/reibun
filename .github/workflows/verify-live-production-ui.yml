name: Verify live production UI

on:
  push:
    branches: [verify-live-production-ui]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm install --no-save playwright
      - run: npx playwright install --with-deps chromium
      - run: node scripts/verify-live-ui.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: live-production-ui-verification
          path: audit/live-ui
          retention-days: 7
